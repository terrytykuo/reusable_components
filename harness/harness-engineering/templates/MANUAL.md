# 手動操作

這份文件只保留人類必須親手做的事情：安裝依賴、填入外部憑證、啟動外部服務，以及做需要人工判讀的驗證。

## 人類需要準備的東西

- `[依賴安裝命令]`
- `[必要環境變數]`
- `[外部服務帳號 / 憑證 / 可公開 URL]`

## 最短驗證路徑

1. 安裝依賴：

```bash
[install command]
```

2. 啟動主要服務：

```bash
[run command]
```

3. 健康檢查：

```bash
[health check command]
```

成功條件：
- `[應觀察到的結果]`

## 外部服務驗證路徑

1. 設定必要憑證與環境變數
2. 啟動服務並確認公開入口可達
3. 依序驗證 webhook / callback / UI / summary / persistence 等關鍵路徑

## 尚未自動化、必須人工判讀的項目

- `[人工才能確認的體驗或外部整合結果]`
- `[語意品質、對話品質、真實設備互動等]`

## 維護文檔時的人類責任

- 改 `AGENTS.md` 後，若 repo 採鏡像機制，記得同步 `CLAUDE.md`
- 若調整 repo structure 或檔案責任，記得同步 `INDEX.md`
- 若調整重要取捨或正式運維邊界，記得同步 `DECISIONS.md`
