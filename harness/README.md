# Harness Components

這個資料夾保留 reusable 的 harness engineering 資產，包含 repo collaboration、agent workflow、shared project memory 與 verification workflow 相關元件。

目前已建立的相關 starter kit 可參考：

- `../skill/harness-engineering/`

後續若要把更多 harness 模板、bootstrap prompt 或 workflow 文件抽到 top-level，可繼續放在這裡。
